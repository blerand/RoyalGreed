<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.blerandbahtiri.royalgreed"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--  android:theme="@style/Blerand"> -->
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".GameActivity"
                  android:parentActivityName="com.example.blerandbahtiri.royalgreed.IntroActivity" >
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.blerandbahtiri.royalgreed.IntroActivity" />
        </activity>
        <activity
            android:name=".FinalActivity"
            android:parentActivityName="com.example.blerandbahtiri.royalgreed.IntroActivity" >
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.blerandbahtiri.royalgreed.IntroActivity" />
        </activity>
    </application>

</manifest>
